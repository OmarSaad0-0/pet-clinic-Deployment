define service{
      use                     generic-service
      host_name               localhost
      service_description     sanity checks for pet-clinic application
      check_command           check_http! -H localhost -p 9090 -u /spring-petclinic/ -w 10 -c 40 
    }
define service{
      use                     generic-service
      host_name               localhost
      service_description     sanity checks for pet-clinic application
      check_command           check_http! -H localhost -p 9091 -u /spring-petclinic/ -w 10 -c 40 
    }
